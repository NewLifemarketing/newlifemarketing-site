/* NewLife Portal config — TEMPLATE.
   Copy this file to  portal/config.js  and paste your two Supabase values.
   Both are browser-safe (the anon key is meant to be public; Row-Level
   Security protects the data). Leave them blank to run in demo mode. */
window.PORTAL_CONFIG = {
  SUPABASE_URL: "",       // e.g. https://abcd1234.supabase.co   (Supabase → Project Settings → API)
  SUPABASE_ANON_KEY: ""   // the "anon / publishable" key         (same page)
};
